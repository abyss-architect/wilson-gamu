public class Dagger extends CloseRange {
  
  // Constructor.
  public Dagger(String itemName, String itemDescription, int weaponDamage, int attackSpeed, int attackRange) {
    super(itemName, itemDescription, weaponDamage, attackSpeed, attackRange);
  }
}