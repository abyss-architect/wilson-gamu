public class LongSword extends CloseRange {
  
  // Constructor.
  public LongSword(String itemName, String itemDescription, int weaponDamage, int attackSpeed, int attackRange) {
    super(itemName, itemDescription, weaponDamage, attackSpeed, attackRange);
  }
}